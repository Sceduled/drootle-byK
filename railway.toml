[build]
builder = "NIXPACKS"

[build.buildCommand]
value = "cd frontend && npm install && npm run build && cd .."

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
